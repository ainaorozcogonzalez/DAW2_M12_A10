document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('userForm');
    if (!form) {
        console.error('No se encontró el formulario con ID "userForm"');
        return;
    }

    // ... existing code ...
}); 